// Vitest global setup for the Popup_UI test suite.
//
// Extends Vitest's `expect` with jest-dom matchers (e.g. toBeInTheDocument)
// so React component tests can assert against the rendered DOM.
import "@testing-library/jest-dom/vitest";
